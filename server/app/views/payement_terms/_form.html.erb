<%= form_for(@payement_term) do |f| %>
  <% if @payement_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payement_term.errors.count, "error") %> prohibited this payement_term from being saved:</h2>

      <ul>
      <% @payement_term.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
